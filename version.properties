# Human-edited. Major and Minor are set here; Patch (commits since last Minor bump) and
# Build (git commit count + VERSION_CODE_OFFSET) are derived from git in app/build.gradle.kts.
versionMajor=0
versionMinor=9
