<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base window theme used once Compose takes over. Dark, no action bar. -->
    <style name="Theme.Guillotine" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Launch theme (androidx core-splashscreen): the provided app icon on black,
         then hands off to Theme.Guillotine once the activity is ready. -->
    <style name="Theme.Guillotine.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/guillotine_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.Guillotine</item>
    </style>
</resources>
